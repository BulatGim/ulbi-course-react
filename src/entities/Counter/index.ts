import { Counter } from './ui/Counter';
import { counterReducers } from './model/slices/CounterSlice';
import { CounterSchema } from './model/types/CounterSchema';

export {
    CounterSchema,
    counterReducers,
    Counter,
};
